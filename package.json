{
  "name": "parkour-hero",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "preview": "vite preview",
    "build": "rimraf dist release && vite build",
    "version": "npm run build"
  },
  "devDependencies": {
    "vite": "^7.0.5",
    "rimraf": "^5.0.7",
    "vite-plugin-zip-pack": "^1.1.2"
  },
  "dependencies": {
    "lit": "^3.1.3"
  }
}