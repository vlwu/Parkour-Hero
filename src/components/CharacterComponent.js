export class CharacterComponent {
    /**
     * @param {string} characterId The ID of the character skin (e.g., 'PinkMan').
     */
    constructor(characterId) {
        this.characterId = characterId;
    }
}